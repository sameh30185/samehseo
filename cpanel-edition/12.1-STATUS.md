# SAMEH 12.1 Professional — Status

**Version:** 12.1.0-dev  
**Branch:** `dev/12.1-professional`  
**Base:** extends `cpanel-edition` from `rc/12.0.0-rc1` (no rebuild from scratch)

## Done — Phase A
- [x] `isInstalled()` requires `COUNT(users)>0`; empty users allow `/install`
- [x] Owner password recovery (forgot link, opaque responses, hashed token, TTL 30m, one-time, rate limit, audit without secrets)
- [x] Reset form; revoke sessions via `session_version`; keep 2FA
- [x] Mailer interface + SMTP; fallback `storage/mail-outbox` + same success UX
- [x] Settings change password (+ TOTP if enabled)
- [x] Emergency recovery key via `emergency_recovery_key_file` (constant-time)
- [x] Migrator + `sql/migrations/001_12_1_foundation.sql` (additive only)
- [x] Active site (`active_site_id`, layout switcher, `App::activeSite()`)

## Done — Phase B
- [x] `app/AI/ProviderClient.php` OpenAI-compatible; encrypted API key; test connection; cloud OFF default; redact
- [x] `docs/AI-GATEWAY.md` Hermes remote gateway notes
- [x] Company agents + Director (deterministic evidence analysis; optional LLM enrich)
- [x] Missions UI+API: create/list/detail/run/cancel/resume; site-scoped; parallel duplicate blocked; Arabic summary
- [x] Factory/Growth: minimal real “create mission” + Phase C disabled buttons (no fake unfinished features)

## Remaining — Phase C (TODO)
- [ ] Typed actions + Preview/Approve/Execute/Verify/Rollback via Connector (draft-only safe path)
- [ ] Factory draft planner (full)
- [ ] Growth opportunity list from evidence (full)
- [ ] Owner-machine Hermes job claim API polish
- [ ] Richer Discover→evidence continuous sync UI

## Tests
- `php tests/run.php` — unit/smoke (no MySQL required for suite)
- Covers: recovery contracts, isInstalled source contract, site isolation queries, mission create types, agents deterministic, SecretBox, migrator additive checks

## Build
- `dist/build-release.sh` → `SAMEH-12.1-professional.zip` + connector zip

## Non-goals / preserved
- Sites pairing columns untouched (additive migrations only)
- Zero Composer for production
