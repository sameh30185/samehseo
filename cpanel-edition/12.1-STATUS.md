# SAMEH 12.1 Professional — Status

**Version:** 12.1.0-rc1  
**Branch:** `dev/12.1-professional`  
**Base:** extends `cpanel-edition` from `rc/12.0.0-rc1` (no rebuild from scratch)

## Done — Phase A
- [x] `isInstalled()` requires `COUNT(users)>0`; empty users allow `/install`
- [x] Owner password recovery (forgot link, opaque responses, hashed token, TTL 30m, one-time, rate limit, audit without secrets)
- [x] Reset form; revoke sessions via `session_version`; keep 2FA
- [x] Mailer interface + SMTP; fallback `storage/mail-outbox` + same success UX
- [x] Settings change password (+ TOTP if enabled)
- [x] Emergency recovery key via `emergency_recovery_key_file` (constant-time)
- [x] Migrator + `sql/migrations/001_12_1_foundation.sql` (additive only)
- [x] Active site (`active_site_id`, layout switcher, `App::activeSite()`)

## Done — Phase B
- [x] `app/AI/ProviderClient.php` OpenAI-compatible; encrypted API key; test connection; cloud OFF default; redact
- [x] `docs/AI-GATEWAY.md` Hermes remote gateway notes
- [x] Company agents + Director (deterministic evidence analysis; optional LLM enrich)
- [x] Missions UI+API: create/list/detail/run/cancel/resume; site-scoped; parallel duplicate blocked; Arabic summary
- [x] Factory/Growth: create mission entry points (Phase C completes planners)

## Done — Phase C
- [x] Migration `002_12_1_actions.sql` — `action_plans`, `typed_actions`, approvals columns, `factory_plans`, `growth_opportunities` (additive; no DROP sites/HMAC)
- [x] TypedActionRegistry whitelist + validators (6 typed actions only)
- [x] ActionPlanner / PreviewService / ApprovalService / ExecutionService / VerifyRollbackService
- [x] Preview → Owner Approval (CSRF + optional TOTP) → Execute → Verify → optional Rollback
- [x] Default site mode READ_ONLY refuses execute (Arabic error); Kill Switch blocks; temporary elevate audited
- [x] Connector: HMAC `create_draft`, `update_draft`, `get_post`, `update_rank_math`, `change_post_status` (publish needs confirm + high-risk flag)
- [x] Factory draft planner: templates, conflict checks, shortcode/H1 QA, creates draft action plans
- [x] Growth: derive/score/sort opportunities from Discover; → Mission or single draft plan (no mass create)
- [x] Mission `decision_ready` + UI «إنشاء خطة إجراءات»
- [x] Approvals + Plans UI routes (Arabic RTL)

## Remaining / gaps
- [ ] Owner-machine Hermes job claim API polish
- [ ] Richer Discover→evidence continuous sync UI
- [ ] Live WP execute E2E against paired site (suite is unit/smoke without MySQL/WP)
- [ ] Rollback restores content only when `before_json` captured draft bodies from get_post
- [ ] Large-batch UX polish beyond `needs_extra_approval` flag

## Tests
- `php tests/run.php` — unit/smoke (no MySQL required)
- Phase C adds ≥15 assertions (whitelist, gates, QA, growth sort, migration 002 additive, routes)

## Build
- `dist/build-release.sh` → `SAMEH-12.1-professional.zip` + connector zip

## Non-goals / preserved
- Sites pairing columns untouched (additive migrations only)
- Zero Composer for production
- Cloud AI OFF by default
